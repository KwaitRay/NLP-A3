# COMP90042 Assignment 3 — Hybrid RAG Climate Fact-Checker
# ────────────────────────────────────────────────────────────
# 在 conda env 里 pip 安装：
#   conda create -n nlp_a3 python=3.10 -y
#   conda activate nlp_a3
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_sm
#   python -c "import nltk; nltk.download('wordnet'); nltk.download('omw-1.4')"
#
# AutoDL 国内环境加速：
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 注：torch / cuda 由 AutoDL 镜像预装，本文件不锁版本（避免覆盖镜像）。
# 如本地无 GPU，单独装：pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── 训练框架（Qwen3.5 SFT + DPO）─────────────────────────
ms-swift>=3.0.0
modelscope>=1.20.0
transformers>=4.46.0,<4.50.0   # 4.50+ 引入 CVE-2025-32434 检查，要 torch≥2.6 才能加载 .bin 权重
peft>=0.13.0
trl>=0.12.0
accelerate>=1.0.0
bitsandbytes>=0.44.0           # QLoRA 4-bit 量化

# ── 检索 ───────────────────────────────────────────────
sentence-transformers>=3.0.0   # bge-m3 dense + bge-reranker-base
bm25s>=0.2.0                   # 稀疏检索（不要用 rank_bm25，会 OOM）
pystemmer>=2.2.0               # bm25s 词干处理
faiss-cpu>=1.7.4               # 稠密索引；GPU 版本需要 conda 装 faiss-gpu

# ── NLP 工具 ────────────────────────────────────────────
spacy>=3.7.0                   # NER 实体抽取（rule reorder 用）
nltk>=3.9.0                    # WordNet 同义词扩展

# ── 数据处理 / 评测 ─────────────────────────────────────
numpy>=1.24,<2.0               # transformers 还不兼容 numpy 2.x
scipy>=1.11.0
scikit-learn>=1.3.0
pandas>=2.0.0
tqdm>=4.66.0

# ── Notebook 运行时 ─────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.25.0
ipywidgets>=8.0.0              # tqdm 进度条 + transformers 显示
