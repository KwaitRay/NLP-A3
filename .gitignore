__pycache__/
*.pyc
*.pyo
*.egg-info/
.ipynb_checkpoints/

# Large data files / artifacts (never commit)
data/evidence.json
outputs/sft_data/*.jsonl
outputs/splits/*.jsonl
outputs/eda/*.png
outputs/checkpoints/
outputs/embeddings/
outputs/bm25_index/
outputs/dense_index/
outputs/model_cache/
outputs/sft-out/
outputs/dpo-out/
outputs/predictions/
outputs/smoke/
# Phase 1 prediction dumps — per-claim model outputs, large + regenerable.
# Eval-report markdowns (track*_*.md, summary_*.md, diagnose_*.md,
# retrieval_ceiling_*.md) under outputs/eval_phase1/ stay tracked per
# optimization_plan.md §7.3.
outputs/eval_phase1/*.json
# Third-party model weights — downloaded via scripts.download_models on each box.
# Inline-comment form (`models/ # ...`) doesn't work in .gitignore patterns —
# must keep the comment on its own line above.
models/
*.safetensors
*.bin
*.pt
*.pth
*.gguf
*.npy

# Submission packaging
*.zip

# Local env
.env
.venv/
venv/

# Claude Code workspace (plans / personal config — not for sharing)
.claude/
